const PORT_BACKEND = 8090;
const URL_BACKEND = "http://localhost:" + PORT_BACKEND.toString();

const PORT_FRONTEND = 3000;
const URL_FRONTEND = "http://localhost:" + PORT_FRONTEND.toString();

module.exports = {PORT_BACKEND, URL_BACKEND, PORT_FRONTEND, URL_FRONTEND}